# Test Environment Configuration
NODE_ENV=test

# MongoDB Test Database
# For testing, use a local MongoDB instance or a separate test cluster
ATLAS_DB=mongodb://localhost:27017/ednexus_test
# JWT Secret for testing
JWT_SECRET=test_jwt_secret_key_for_development_only_123456789